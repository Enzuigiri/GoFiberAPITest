kind: pipeline
type: docker
name: default

steps:
- name: Testing
  image: golang:1.20-alpine
  commands:
    - "cd ./src && go build"
    - "go test -v"
    - "go test ../test -v"

- name: build
  image: docker:20
  commands:
    - docker build -t my-golang-app .

- name: run
  image: docker:20
  commands:
    - docker run -d -p 8080:8080 my-golang-app